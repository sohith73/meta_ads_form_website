# Production (also the code default when unset)
VITE_API_BASE_URL=https://api.flashfirejobs.com

# Local backend for testing (flashfire-website-backend runs on PORT=5000)
# VITE_API_BASE_URL=http://localhost:5000
